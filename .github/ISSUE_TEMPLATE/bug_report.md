---
name: Bug report
about: Report something that doesn't work as documented
labels: bug
---

**What happened**
A clear description of what went wrong.

**What I expected**
What you thought would happen.

**Steps to reproduce**
1. ...
2. ...

**Environment**
- Commit: (`git rev-parse HEAD`)
- Installed with: Docker / manually
- PHP version: (`php -v`)
- MySQL version: (`mysql --version`)
- Web server: (Apache, nginx + PHP-FPM, ...)

**Errors**
```
# with DEBUG=1: the error output, the Server-Timing header or the query panel, plus the web server log;
# redact anything private
```
